name: spraxa_tech_assesment
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  async_redux: ^18.0.2
  country_picker: ^2.0.17
  csc_picker: ^0.2.6
  cupertino_icons: ^1.0.2
  firebase_analytics: ^10.0.6
  firebase_auth: ^4.1.3
  firebase_crashlytics: ^3.0.6
  firebase_core: ^2.3.0
  flutter_native_splash: ^2.2.15
  font_awesome_flutter: ^10.2.1
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/kundli.png
    - assets/kundli_logo.png
    - assets/kundl_2.png
